Replace custom loading spinners with shadcn Skeleton component

- Add shadcn Skeleton component with pulse animation
- Replace Loader2 spinner in invoices/[id]/loading.tsx with skeleton matching invoice page structure
- Replace loading spinner in ClientsTab with skeleton table rows
- Replace loading spinner in InvoicesTab with skeleton table rows matching invoice table structure
- Replace loading spinner in ProjectsTab with skeleton cards matching project card layout
- Replace loading spinner in DocumentsTab with skeleton table rows
- Replace loading spinner in client-portal/page.tsx with skeleton sections for projects, documents, and invoices
- Remove unused Loader2 imports from all affected files

All loading states now use Skeleton components that match the final content structure, providing better UX feedback and reducing layout shift.

