import LoginPage from "@/components/pages/login";

export const metadata = {
  title: "Client Login | Matte Digital",
  description: "Sign in to access your client portal and invoices",
};

export default function Page() {
  return <LoginPage />;
}
